# Copyright 2026 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

apiVersion: v1
kind: Namespace
metadata:
  name: uds-labs-vms
  labels:
    app.kubernetes.io/part-of: uds-labs
    # All lab VMIs, their Services, and NetworkPolicies live here (ADR-0011).
    # Kept separate from the platform server/operator namespace so RBAC and
    # NetworkPolicy can be scoped tightly.
