# Copyright 2026 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later

# UDS Package Kit release metadata. `uds run release-tasks:prepare` uses the
# Package Kit-rendered version to synchronize the Labs package, container image,
# Helm chart, and bundle in the release checkout. VM images are versioned separately.
flavors:
  - name: upstream
    version: v0.6.1-uds.0
