# Maps email domains to customer display names and assigned CSEs.
# Add an entry here when onboarding a new customer org.
#
# Example:
#   acme.com:
#     display_name: ACME Corp
#     cse: Jordan M.

# Keycloak group whose members can access the admin UI.
# Must match the exact group path as reported in X-Auth-Request-Groups.
ae_group: "/UDS Core/Admin"

customers: {}
